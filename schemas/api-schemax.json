{
    "type": "object",
    "properties": {
        "name": {
            "maxLength": 60,
            "minLength": 1,
            "type": "string",
            "description": "Provide a name for the api. Name has to be unique. It should be an identifier.",
            "enum": ["api_name"]
        },
        "context": {
            "maxLength": 232,
            "minLength": 1,
            "type": "string",
            "description": "Provide the context for the api. Context has to be unique. It should be an identifier.",
            "enum": ["api-context"]
        }
    }
  }
  